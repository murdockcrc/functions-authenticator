# Azure Functions authenticator

Sample code for using Azure Functions to register and authenticate IoT Hub devices.

Each function has a file named _test.js_, which includes unit testing for the function. Simply run this test file with mocha and watch the results.
